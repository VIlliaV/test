//Клонування:
git clone https://github.com/USER name/repoNAME.git 
//Клонування з новою назвою:
git clone https://github.com/USER name/repoNAME.git NEWrepoNAME 
//Перейти в папку:
CD NAMEfolders
//Відкрити проект у VSCode:
Code .
//Версія Node:
node -v
//Запуск інсталяції пакетів для Parcel
npm i
//Запуск режиму розробки:
npm start
//Зупинка сервера
ctrl+C
// Build Project (Побудова проекту) Збирає все "до купи". Картинки - оптимізувати НЕ ТРЕБА. оптимізовує цей сборщик. 
!!побудова робиться автоматично на ГІТІ (можна не використовувати цю функцію)!!
npm run build
//Подивитись за яким шляхом знаходиться репозиторій на ГІТІ
git remote-v
// Змінити шлях до репозиторія:
git remote set-url origin https://github.com/USER name/repoNAME.git
// Збираєм всі зміни в один коміт
git add .
// додаєм ім'я коміту:
git commit -m "Назва коміта"
// Пушимо коміти
git push origin main
// Пулл коміта :
git pull origin main

REACT PROJECT =============

npx create-react-app .
npm start
git init
git add .
git commit -m "Перший коміт"
git push
npm install gh-pages --save-dev

package.json=====
"homepage": "https://<ваш_гітхаб_логін>.github.io/<назва_репозиторію>",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
}
